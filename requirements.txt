# Core compatibility (needed for PaddleOCR)
numpy==1.23.5
protobuf==3.20.2

# OCR stack
paddleocr==2.7.0.3
paddlepaddle==2.6.2

# Image processing
opencv-python==4.6.0.66
pillow==10.4.0

# AI / LLM
google-genai==1.65.0

# Environment variables
python-dotenv==1.0.1

# Build support (prevents install warnings)
wheel
setuptools
